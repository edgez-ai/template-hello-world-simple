# React Native

The Expo app renders a centered `Hello World` screen.

```sh
npm install
npm run android
```

`npm run android` starts Metro and opens the project in EdgeZ Android DevTools
at `127.0.0.1:5555`. Override `ANDROID_SERIAL` or `EXPO_PORT` when needed.
